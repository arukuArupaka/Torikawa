# たべどき！！

冷蔵庫の食材と期限を管理し、「食べどき」を逃さず食材ロスを減らすためのスマホアプリです。React Native + Expo SDK 54 + TypeScriptで作っているため、1つのコードからiPhoneとAndroidの両方で動かせます。SDK 54は今回使用するiPhone版Expo Goの「Supported SDK 54」に合わせています。

このリポジトリは、最初から機能を詰め込みすぎない「動く最小版」から育てている学習用アプリです。食材、買い物リスト、通知設定、レシピ、学習済みバーコード商品は、サーバーではなく端末内のAsyncStorageへ保存します。

## できること

- 期限別の食材一覧、検索、追加、編集、詳細、削除
- 商品名とレシピ用の食材名を分けて管理
- `明治おいしい牛乳`のような商品名を、レシピ判定では`牛乳`として扱う分類補正
- 食材名、商品名、レシピ用分類、タグを使ったホーム検索
- 食材カードをしっかりスワイプすると確認が開く「使い切った」「捨てた」の履歴記録
- 一覧とカード内のスワイプヒント、端末ごとに初回だけ表示する操作ガイド
- 今月の使い切り数、廃棄数、食材ロス率の統計と記録の取り消し
- 今月の使い切り・廃棄比率を表示する円グラフ
- 全期間の廃棄記録から集計する「よく捨てているもの」ランキング
- ホーム左上の統計アイコンから統計画面へ直接移動
- 統計の「今月・先月・全期間」切り替えと、件数・円グラフ・ランキング・履歴の期間連動
- 月単位の食材ロス率を直前の月と比較する改善表示
- 廃棄ランキングから「次回は少なめに買う」メモ付きで買い物リストへ追加
- 買い物リストの注意メモ表示と、未完了項目の重複追加防止
- 過去の登録食材と購入済みリストから作る「よく買うもの」候補
- 買い物リストを購入済みにした後、食材名を引き継いで登録画面へ移動
- 食材を使い切った時に、同じ食材を買い物リストへ追加する選択肢
- 食材カテゴリの登録・編集と、既存食材のカテゴリ自動補完
- 食材名から画像、保存場所、期限日の目安、数量と単位を自動補完
- ホーム一覧の「期限順・保存場所別・カテゴリ別」表示切り替え
- 登録食材から計算するレシピの「作れる度」と不足食材表示
- 期限が近い未期限切れ食材を使えるレシピの優先表示
- レシピの不足食材をメモ付きで買い物リストへまとめて追加
- レシピ詳細で材料の分量、調理手順、登録在庫を確認
- 調理後に食材ごとに「使い切る・1個減らす・変更なし」を選択
- レシピ調理の確定と連動した食材状態・数量・通知・統計の更新
- レシピ調理で使い切った食材を、まとめて買い物リストへ追加
- 料理画像、料理名、時間、複数材料・分量、複数手順を入力するレシピ追加
- 登録レシピの編集・削除と、手順の並べ替え
- レシピのお気に入り切り替え・絞り込み
- サンプルとユーザー作成レシピの端末内保存
- 食材・履歴・買い物・通知・レシピ・学習済みバーコード商品のJSONバックアップ
- 端末保存画像を含むバックアップの共有と、別端末での復元
- 食材写真の撮影・ライブラリ選択と端末内保存
- iPhone・Android標準の日付ピッカー
- 冷蔵・冷凍・常温の保存場所選択
- 食材数量の登録・表示・編集
- 個数、g、ml、本、袋、パックの単位管理
- 期限日を表示する月間カレンダー
- カレンダーの日付タップによる、その日が期限の食材一覧
- 選択中の日付をもう一度タップして食材一覧を閉じる操作
- 選択日を期限にした食材追加と、カレンダー上での期限日変更
- 実機カメラによるJAN・EAN・UPC・Code 128バーコード読み取り
- バーコード商品も期限日・購入日・保存場所・数量を確認して登録
- 初回登録したバーコード商品を端末へ学習し、2回目以降に自動表示
- バーコード商品名からレシピ用分類、カテゴリ、タグを自動推測
- 自動推測が違う場合、登録画面でレシピ用分類を手動修正
- 同じバーコードでは、前回ユーザーが修正した分類を優先
- 登録食材を使うサンプルレシピ
- 期限が近い食材を使ったクックパッド検索ページへの安全な外部リンク
- 追加、チェック、削除ができる買い物リスト
- 通知ON/OFF、複数の通知日、通知時刻の設定保存
- 食材の追加・編集・削除と連動する期限ローカル通知
- 約3秒後に確認できるテスト通知
- 通知タップから該当食材の詳細画面を表示
- 通知権限と予約済み期限通知件数の表示

商品検索は、学習済み端末内カタログ、固定サンプル、Yahoo!ショッピング商品検索API、Open Food Factsの順に確認します。クックパッド連携は検索ページを開くだけで、レシピ本文や画像の取得、スクレイピング、非公式API利用はしません。期限通知は端末内で動作するローカル通知として実装しています。

## 必要なソフト

1. [Node.js](https://nodejs.org/) 24 LTS
2. [Visual Studio Code](https://code.visualstudio.com/)
3. スマホの「Expo Go」アプリ
4. パソコンとスマホを接続する同じWi-Fi

WindowsではPowerShellの設定によって`npm`が実行できない場合があります。このREADMEでは、その影響を受けにくい`npm.cmd`と`npx.cmd`を使います。macOS/Linuxでは`.cmd`を外してください。

## VS Codeで開く

PowerShellで次を実行します。

```powershell
cd C:\Users\user\Documents\torikawa
code .
```

`code`が見つからない場合は、VS Codeを起動し、`ファイル` → `フォルダーを開く`からこのフォルダーを選びます。

## インストール

すでにパッケージは導入済みです。別のパソコンで開いた場合や`node_modules`がない場合だけ実行します。

```powershell
npm.cmd install
```

Yahoo!ショッピング商品検索APIを使う場合は、`.env.example`をコピーして`.env`を作ります。`.env`はGitに上げないローカル設定ファイルです。

```powershell
Copy-Item .env.example .env
```

最初から手動でパッケージを追加する場合のコマンドは次のとおりです。`expo install`は使用中のExpo SDKと互換性がある版を選びます。

```powershell
npx.cmd expo install @react-native-async-storage/async-storage @react-navigation/native @react-navigation/bottom-tabs @react-navigation/native-stack react-native-screens react-native-safe-area-context react-native-gesture-handler react-native-svg @expo/vector-icons expo-font expo-image-picker expo-file-system expo-notifications expo-camera expo-sharing expo-document-picker @react-native-community/datetimepicker
```

## Expoで起動する

```powershell
npx.cmd expo start
```

ターミナルに表示されたQRコードを、iPhoneは標準カメラ、AndroidはExpo Goから読み取ります。

- Androidエミュレーターで開く: ターミナルで`a`
- iOSシミュレーターで開く: macOSで`i`
- 開発メニューを表示: ターミナルで`m`
- 通常接続できない場合: `npx.cmd expo start --tunnel`

WindowsではiOSシミュレーターを起動できませんが、実機iPhoneのExpo Goでは確認できます。

### 「Project is incompatible」と表示された場合

このプロジェクトは端末のExpo Goに合わせてSDK 54に固定済みです。古い開発サーバーを終了してから、キャッシュを消して起動し直してください。

```powershell
npx.cmd expo start --clear
```

Expo Goを削除して入れ直す必要はありません。ターミナルに新しく表示されたQRコードを読み取ってください。

## ファイル構成

```text
torikawa/
├─ App.tsx                         # アプリ全体の入口
├─ app.json                       # Expoのアプリ名、アイコン設定
├─ src/
│  ├─ components/                # ボタン、日付・写真入力、食材カードなど共通UI
│  │  └─ BarcodeScanner.tsx      # 実カメラとバーコード読み取り
│  │  └─ SwipeableFoodCard.tsx   # 左右スワイプできる食材カード
│  ├─ constants/theme.ts         # 色、余白、角丸
│  ├─ context/AppDataContext.tsx # データ操作と端末保存
│  ├─ data/                      # 初回食材、レシピ、固定バーコード商品
│  ├─ navigation/                # 下部タブと画面遷移、画面の型
│  ├─ screens/                   # 画面ごとのファイル
│  │  └─ StatisticsScreen.tsx    # 使い切り・廃棄数と食材ロス率
│  ├─ services/                  # 端末保存、通知、商品検索、バックアップ
│  ├─ types/index.ts             # FoodItemなどのTypeScript型
│  └─ utils/                     # 日付計算、ID生成、食材分類、レシピ判定、買い物候補
└─ assets/                        # アプリアイコン、サンプルレシピ画像
   └─ recipes/                    # デフォルトレシピ用の料理画像
```

画面を直す場合は`src/screens`、色や余白を直す場合は`src/constants/theme.ts`、共通の見た目を直す場合は`src/components`から探すと迷いにくい構成です。

## 実装した順番

1. 画面遷移と下部タブバー
2. ホーム画面と食材追加、端末保存
3. 食材詳細と削除
4. 月間カレンダーと近日予定
5. バーコード登録画面と実カメラ読み取り
6. レシピ画面、クックパッド検索、作れる度判定
7. 買い物リストと通知設定
8. 白背景、緑色、カード中心のiPhone風デザイン

この順にGitコミットを分けて学習しても構いません。現在のコードは8段階を土台に、統計、通知、バックアップ、バーコード商品学習、レシピ改善まで追加した状態です。

## 第2段階で追加した機能

1. 食材詳細から登録内容を編集
2. カメラ撮影または写真ライブラリから食材画像を設定
3. 選択画像をアプリ専用領域へ保存
4. 期限日と購入日をOS標準の日付ピッカーで選択

写真を初めて使うときは、iPhoneまたはAndroidの確認画面でカメラ・写真へのアクセスを許可してください。

## バーコード登録の使い方

1. バーコードタブを開く
2. 初回はカメラの使用を許可する
3. 商品のJAN・EANバーコードを緑色の枠内へ映す
4. 商品情報を確認して登録画面へ進む
5. 期限日・購入日・保存場所・数量を入力または修正して保存する

同じコードの連続読み取りは自動で停止します。「もう一度読み取る」でカメラを再開できます。商品情報は、学習済み端末内カタログ、固定サンプル、Yahoo!ショッピング商品検索API、Open Food Factsの順に探します。見つからない場合は読み取った番号をメモへ引き継いで手入力できます。「サンプル商品で動作を試す」ではカメラを使わず登録フローを確認できます。

バーコードからは期限日・購入日・保存場所・数量を取得しません。商品が見つかった場合も自動保存せず、必ず入力画面でユーザーが確認してから登録します。アップデート前に保存した食材の数量は自動的に1として扱います。

バーコード登録では、商品名とレシピ用分類を分けて保存します。たとえば商品名が`明治おいしい牛乳`でも、レシピ用分類は`牛乳`として保存できます。登録画面で分類を手動修正した場合は、そのバーコードに対する分類情報も端末へ保存し、次回同じバーコードを読み取ったときに優先して使います。

商品名からレシピ用分類を自動推測するルールには、牛乳、ヨーグルト、卵、豆腐、納豆、チーズ、パン、米に加えて、ベーコン、ソーセージ、ツナ、トマト缶、玉ねぎ、じゃがいも、パスタ、醤油、味噌、めんつゆなどを登録しています。

オンライン検索にはインターネット接続が必要です。Yahoo!ショッピングとOpen Food Factsのデータは、商品によって未登録または不完全な場合があります。取得元は読み取り結果に表示します。

## レシピ機能の使い方

レシピ画面では、登録されている管理中の食材をもとに「作れる度」を計算します。判定では商品名ではなく、`ingredientName`と`tags`を優先します。

例:

- 商品名: `明治おいしい牛乳`
- レシピ用分類: `牛乳`
- タグ: `牛乳`, `乳製品`, `飲み物`

この場合、レシピの必要材料が`牛乳`なら「持っている」と判定されます。期限切れの食材はレシピ候補から除外し、期限が近い食材を使えるレシピを優先します。

レシピ詳細では、調理後に食材ごとに次の操作を選べます。

- 変更なし
- 使用量を入力して残量を減らす
- 全部使い切る

クックパッド連携は、期限が近い管理中の食材から検索キーワードを作り、クックパッドの検索ページを外部ブラウザで開きます。レシピ本文や画像をアプリ内に取り込むことはしません。

## 期限通知の使い方

1. 設定タブを開く
2. 数字を入力して「追加」を押し、通知日を必要な数だけ登録する
3. 通知時間を決める（`0日前`は期限当日）
4. 「通知設定を保存」を押して通知を許可する
5. 「3秒後にテスト通知」で端末に表示されることを確認する（食材登録済みなら、通知タップで詳細画面も確認できます）

追加した通知日はチップをタップすると削除できます。食材の追加・編集・削除後は、予約済みの期限通知も自動で更新されます。通知をOFFにして保存すると、このアプリが予約した期限通知を解除します。以前の単一通知設定は、その日数を1件含む複数通知設定へ自動移行します。

期限通知をタップすると該当食材の詳細画面が開きます。アプリが開いている場合、バックグラウンドの場合、終了している場合のいずれにも対応しています。食材が既に削除されている場合はホーム画面を表示します。設定画面の「通知の状態」では、通知権限と現在予約中の期限通知件数を確認できます。

## バックアップと復元

1. 設定タブの「データ管理」を開く
2. 「バックアップを書き出す」を押し、JSONファイルをファイルアプリやクラウドへ保存する
3. 復元する端末で「バックアップから復元」を押す
4. 保存したJSONを選び、作成日時と件数を確認して「復元する」を押す

端末内に保存した食材・レシピ・バーコード商品の画像は、JSON内へ含めて別端末で再生成します。そのため画像が多い場合はファイルサイズが大きくなります。復元すると現在の主要データは置き換わるため、先に現在データを書き出してください。通知権限は端末固有なのでバックアップせず、復元後の食材と通知設定から通知予約だけを作り直します。

## 主なデータの場所

`FoodItem`は`src/types/index.ts`にあり、次の値を持ちます。

```ts
type FoodItem = {
  id: string;
  name: string;
  productName: string | null;
  ingredientName: string;
  image: string;
  category: 'vegetables' | 'meat' | 'seafood' | 'dairy' | 'eggs' | 'staples' | 'seasonings' | 'beverages' | 'other';
  tags: string[];
  storage: 'refrigerated' | 'frozen' | 'room';
  quantity: number;
  quantityUnit: 'piece' | 'g' | 'ml' | 'bottle' | 'bag' | 'pack';
  expiryDate: string;
  purchaseDate: string;
  memo: string;
  createdAt: string;
  status: 'active' | 'used' | 'disposed';
  usedAt: string | null;
  disposedAt: string | null;
};
```

`name`はこれまで通りの表示名です。バーコード商品では`productName`に正式な商品名、`ingredientName`にレシピ判定で使う一般的な食材名を保存します。画面表示は`productName`があれば優先し、レシピ判定は`ingredientName`と`tags`を優先します。

日付は`YYYY-MM-DD`形式です。以前から保存されている食材に`status`、`productName`、`ingredientName`、`tags`、`quantityUnit`がない場合は、読み込み時に自動補完します。端末保存のキーと処理は`src/services/storage.ts`、既存データの補完は`src/context/AppDataContext.tsx`にまとめています。

## 使い切り・廃棄記録の確認

1. 食材カードを普通にタップし、これまで通り詳細画面が開くことを確認する
2. 詳細画面の「使い切った」を押し、確認で「記録だけ」を選ぶとホームから消えることを確認する
3. 別の食材で「捨てた」も同様に確認する
4. 確認画面で「キャンセル」を押した場合は、ホームに食材が残ることを確認する
5. ホームのカードを右へしっかりスワイプし、ボタンを押さずに「捨てた」の確認が開くことを確認する
6. ホームのカードを左へしっかりスワイプし、ボタンを押さずに「使い切った」の確認が開くことを確認する
7. 「買い物リストにも追加」を選ぶと、記録後に未完了の買い物リストへ同じ食材が追加されることを確認する
8. 同じ食材が未完了の買い物リストにある場合は、重複して追加されないことを確認する
9. カレンダーを開き、記録済みの食材が日付ラベルと近日予定から消えることを確認する
10. 設定の「食材ロス統計」を開き、今月の2種類の件数とロス率を確認する
11. ロス率が `捨てた数 ÷ (使い切った数 + 捨てた数) × 100` になり、記録0件では`0.0%`になることを確認する
12. 統計の「取り消す」で管理中へ戻り、ホームとカレンダーへ再表示されることを確認する
13. Expo Goを閉じて開き直し、記録状態と統計が残っていることを確認する

「削除する」は履歴に残らない完全削除です。間違えて登録したデータだけに使用します。「捨てた」は赤色にし、スワイプ完了だけでは記録を確定せず、必ず確認ダイアログを表示します。スワイプ操作の説明は初回だけ表示され、「OK」を押すと端末内に確認済みとして保存されます。

## 買い物リストの確認

1. 買い物リストを開き、「よく買うもの」が表示されることを確認する
2. 候補をタップし、買い物リストに追加されることを確認する
3. 同じ候補をもう一度タップし、未完了項目として重複追加されないことを確認する
4. 未購入の項目をチェックし、食材登録画面へ食材名が引き継がれることを確認する

## 動作チェック

```powershell
npm.cmd run typecheck
npx.cmd expo-doctor
```

TypeScriptのエラーがないことと、Expoの依存関係が正しいことを確認できます。

## 次に追加しやすい機能

- 学習済みバーコード商品の管理・削除画面
- 商品名からレシピ用分類を推測するルールのさらなる追加
- 栄養成分やアレルギー情報の表示
- 生成AIレシピ提案用の中継サーバー
- 食材写真を並べて見られる写真つきグリッド表示
- 家族や友達と共有できるクラウド同期

学習済み商品はAsyncStorageの`learnedProducts`へ保存し、固定サンプルやオンライン検索より先に参照します。
